class Notification {
    constructor(notification_id, spot_id, user_id) {
        this.notification_id = notification_id;
        this.spot_id = spot_id;
        this.user_id = user_id;
    }
}

export default Notification;